export type Inputs = {
    email: string;
    password: string;
    displayName: string;
    confirmPassword: string; 
};

export type Product = {
    image: string,
    title: string,
    description: string,
    price: number
}